# The mutation harness writes its mutant here and deletes it again; a run that is
# interrupted leaves one behind, and it must never be mistaken for real source.
_mutant.js

# Dependencies (this package ships plain JS and has none, but a clone may add some)
node_modules/
pnpm-lock.yaml
package-lock.json

# Editors / OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
